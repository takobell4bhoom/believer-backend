{
  "name": "believer-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "install:all": "npm install --workspaces",
    "test": "npm --workspace backend run test",
    "backend:dev": "npm --workspace backend run dev",
    "backend:migrate": "npm --workspace backend run migrate",
    "backend:seed": "npm --workspace backend run seed",
    "backend:start": "npm --workspace backend run start",
    "backend:test": "npm --workspace backend run test",
    "backend:test:integration": "npm --workspace backend run test:integration"
  }
}
